public class ShoppingCart01 {
    public static void main(String[] args) {
        // Declare and initialize String variables
        String customerName = "Anisa";
        String item = "sweater";
        String size = "M";
        int harga = 47000;
        int jumlah = 2;
        String message;

        // Message variable
        message = customerName + " membeli " + jumlah + " " + item + " ukuran " + size + " " + "dengan total harga: Rp" + (harga + harga) + ".";

        // Print and run the code
        System.out.println(message);
    }
}